[build]
builder = "RAILPACK"

[deploy]
preDeployCommand = ["uv run alembic upgrade head"]
startCommand = "uv run uvicorn magnus.api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
